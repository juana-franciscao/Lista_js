function vogalConsoante() {
    let letra = prompt("Digite uma letra do afabeto:");

    if (letra == "a" || letra == "e" || letra == "i" || letra == "o" || letra == "u") {
        console.log("Essa letra é uma vogal")

    } else {
        console.log("Essa letra é uma consoante");
    }
}
